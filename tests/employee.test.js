
describe("employee", () => {
        
    it("Should include an employee id", () => {

    const id = 1 

    expect(employee.id).toEqual(id)

})});

describe("employee", () => {
        
    it("Should include an employee id", () => {

    const email = 1 

    expect(employee.id).toEqual(id)

})});

describe("employee", () => {
        
    it("Should include an employee email", () => {

    const id = 1 

    expect(employee.id).toEqual(id)

})});